#include "pigment.h"
#include <algorithm>






void MovePigment(/* Wet area mask M, velocity field u, also velocity field v, set of initial pigment concentrations [g0 to gk], int g_width, int g_height */) {
	float delta_t = 1 / std::max(2 /* u */, 3 /* v */);

	//for (int i = 0; i < 5 /* len g array */; i++) {
	//	for()
	//}

}
